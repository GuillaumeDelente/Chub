<resources>

    <!-- Base application theme. -->
    <style name="ChubTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="colorAccent">@color/chub_blue</item>
        <item name="colorControlHighlight">@color/chub_blue</item>
    </style>

    <!-- Base application theme. -->
    <style name="MapTheme" parent="ChubTheme">
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
    </style>

    <style name="DialtactsActionBarStyle" parent="Widget.AppCompat.ActionBar">
        <item name="android:background">@color/actionbar_background_color</item>
        <item name="android:titleTextStyle">@style/DialtactsActionBarTitleText</item>
        <item name="android:height">@dimen/action_bar_height</item>
        <item name="android:elevation">@dimen/action_bar_elevation</item>
        <item name="android:homeAsUpIndicator">@null</item>
        <!-- Empty icon -->
        <item name="android:icon">@android:color/transparent</item>
        <!-- Shift the title text to the right -->
        <item name="android:contentInsetStart">@dimen/actionbar_contentInsetStart</item>
    </style>

    <!-- Text in the action bar at the top of the screen -->
    <style name="DialtactsActionBarTitleText" parent="RtlOverlay.Widget.AppCompat.ActionBar.TitleItem">
        <item name="android:textColor">@color/actionbar_text_color</item>
    </style>

    <style name="ShareActionBarStyle" parent="Widget.AppCompat.ActionBar.Solid">
        <item name="android:background">@color/chub_blue</item>
        <item name="android:titleTextStyle">@style/ShareActionBarTitleText</item>
        <item name="android:height">@dimen/action_bar_height</item>
        <item name="android:elevation">@dimen/action_bar_elevation</item>
        <!-- Empty icon -->
        <item name="android:icon">@android:color/transparent</item>
    </style>

    <!-- Text in the action bar at the top of the screen -->
    <style name="ShareActionBarTitleText" parent="RtlOverlay.Widget.AppCompat.ActionBar.TitleItem">
        <item name="android:textColor">@color/actionbar_text_color</item>
    </style>

    <style name="search_result1" parent="@android:style/TextAppearance">
        <item name="android:textColor">@color/search_result</item>
        <item name="android:textSize">@dimen/search_result1_textsize</item>
        <item name="android:textStyle">bold</item>
    </style>

    <style name="search_result2">
        <item name="android:textColor">@color/search_result_matched</item>
        <item name="android:textSize">@dimen/search_result2_textsize</item>
    </style>

</resources>
